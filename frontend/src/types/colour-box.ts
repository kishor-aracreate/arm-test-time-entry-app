/**
 * Props for the `ColorBox` component.
 *
 * @property {number} index - The position or identifier for the color box, typically used for ordering or referencing.
 * @property {string} text - The label or content to be displayed inside the color box.
 */
export interface ColorBoxProps {
  index: number;
  text: string;
}