/**
 * Represents the configuration for a grid layout.
 *
 * @property cols - The number of columns in the grid.
 * @property rows - The number of rows in the grid.
 */
export interface GridLayout {
  cols: number;
  rows: number;
}